import 'package:flutter_test/flutter_test.dart';

void main() {
  test('debe traer todos los usuarios de la base de datos', () {});

  test('debe traer todos los usuarios de la base de datos con estado activo',
      () {});

  test('debe traer un usuario a partir del id', () {});

  test('debe almacenar un usuario en la base de datos', () {});

  test('debe dar de baja un usuario a partir del id', () {});

  test('no debe guardar un usuario que tiene el mismo dni que otro', () {});
}
