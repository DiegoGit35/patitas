import 'package:flutter/material.dart';

class Pageinicio extends StatelessWidget {
  const Pageinicio({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("SOY UN TITULO; MIRENME"),
      ),
    );
  }
}
